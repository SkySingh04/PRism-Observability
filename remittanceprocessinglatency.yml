groups:
- name: metric
  rules:
  - alert: RemittanceProcessingLatency
    expr: 
    for: 5m
    labels:
      severity: p1
      type: metric
    annotations:
      summary: "RemittanceProcessingLatency"
      description: "Remittance processing is taking longer than expected"
      runbook_url: "[Link to Runbook](https://wiki.example.com/runbooks/remittance-latency)"
