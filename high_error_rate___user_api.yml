groups:
- name: metric
  rules:
  - alert: High Error Rate - User API
    expr: 
    for: 5m
    labels:
      severity: p1
      type: metric
    annotations:
      summary: "High Error Rate - User API"
      description: "User API endpoints are experiencing a high rate of 5xx errors"
      runbook_url: "[Link to Runbook](https://internal.example.com/runbooks/high-error-rate)"
