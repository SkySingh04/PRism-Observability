groups:
- name: metric
  rules:
  - alert: User Not Found Spike
    expr: 
    for: 5m
    labels:
      severity: p2
      type: metric
    annotations:
      summary: "User Not Found Spike"
      description: "Unusually high rate of "User not found" responses"
      runbook_url: "[Link to Runbook](https://internal.example.com/runbooks/not-found-spike)"
