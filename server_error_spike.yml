groups:
- name: metric
  rules:
  - alert: Server Error Spike
    expr: 
    for: 2m
    labels:
      severity: p0
      type: metric
    annotations:
      summary: "Server Error Spike"
      description: "High rate of server errors from the error endpoint"
      runbook_url: "[Link to Runbook](https://internal.example.com/runbooks/server-error-spike)"
